---
permalink: /comm.html
---
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title> 蛏子乐园 </title>
    <link rel="stylesheet" href="/style.css">
    {# marked.js is no longer needed here as Eleventy processes markdown #}

</head>
<body>
    <div class="container">
        <div id="comm-columns">
            <div id="left-column-comm">
                <h2>标签</h2>
                <ul id="tag-list">
                    {# Eleventy will generate tags here #}
                    {% for tag in collections.commPosts | getAllTags %}
                        <li><a href="/tags/{{ tag }}/">{{ tag }}</a></li>
                    {% endfor %}
                </ul>
            </div>
            <div id="right-column-comm">
                <h1><a href="/comm.html">蛏子乐园</a> </h1>
                <div id="comm-posts-list">
                    {# Eleventy will render comm posts here #}
                    {% for post in collections.commPosts | reverse %} {# Assuming commPosts is the collection for _comm_posts #}
                        <div class="comm-post-preview">
                            <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
                            <p class="post-meta">发布日期: {{ post.date | date('yyyy-MM-dd') }} | 标签:
                                {% for tag in post.data.tags %}
                                     <a href="/tags/{{ tag }}/">{{ tag }}</a>{% if not loop.last %}, {% endif %}
                                 {% endfor %}
                            </p>
                            <div>{{ post.templateContent | excerpt | safe }}... <a href="{{ post.url }}">阅读更多</a></div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
        <nav>
            <ul>
                <li><a href="/index.html">[ 返回主页 ]</a></li>
            </ul>
        </nav>
    </div>
    {# No more client-side JavaScript for loading posts or tags #}
</body>
</html>
